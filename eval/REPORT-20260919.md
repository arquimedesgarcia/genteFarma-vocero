# REPORT-20260919 — Fase 2: juez v2 + G4 endurecido + LongCat medido (rama agent-qa, SIN commit)

Continuación de `REPORT-20260918.md`. Todo verificado por ejecución real; nada asumido.

## 1. T7 — Juez v2 (addendum F4): calibración DETENIDA a las 3 iteraciones

Cambios en `buildJudgePrompt` (`src/server/ai/prompts.ts`): REGLA F4 (rechazo honesto/escalado ≠ alucinación), CASOS CONCRETOS (no-encontrado-en-catálogo y pedir texto es correcto; alucinación exige afirmación textual del agente), DESEMPATE (veredicto menos severo; rojo solo con alucinación afirmativa citable) y cita del AGENTE obligatoria en cada hallazgo. Versionado en `eval/judge-prompt-v2.md`.

| Iteración | Controles | Score | Delta |
|---|---|---|---|
| v2.1 (solo F4) | A3 `run_92t8hyrh6ukp84nrzgj5` / B3 `run_3vicr7qpmyb86p5q7477` | 73 / 55 | 18 ❌ |
| v2.2 (+casos) | A4 `run_r7xk2twx87qbf9xlppyd` / B4 `run_swmse05ci4b9lluw3efp` | 82 / 91 | 9 ❌ |
| v2.3 (+desempate) | A5 `run_glp9gkaueq295ck13u9w` / B5 `run_01ehvxbm74yvqmuh5zj9` | 82 / 91 | 9 ❌ |

**Criterio delta < ±8 NO cumplido → detenido según instrucción.** Mejora real: delta 18 → 9; 8/11 casos estables en v2.3. El delta residual son 3 casos límite (`Comprador decidido`, `Preguntón de precios`, `Receta por foto`) donde el agente se comporta idéntico y el 7B alterna rojo/verde: ruido inherente del juez local (Ollama 7B a temperature 0 no es 100% determinista). **Siguiente palanca: juez 14B o hosted, NO más iteraciones de prompt.**

**Lectura de score:** el mismo agente 14B+guardas que scored **27** con juez v1 scores **82-91** con juez v2 → la mayor parte del gap era sobre-marcado del juez (confirmación de F4). Los rojos legítimos por alucinación afirmativa persisten donde corresponde.

## 2. T8 — G4 endurecido (COMPLETADO)

- `agentNEA/app/guards.py`: nueva `TPL_CIERRE_G4` (plantilla constante de cierre de escalado; el LLM ya no redacta).
- `agentNEA/app/turn.py`: el veto G4 (`acusacion_fraude`) ahora reemplaza `final_text` con la plantilla ANTES de enviar, además del handoff inmediato.
- Import de guards verificado OK en host (`python -c` assert). En corridas reales (A5/B5) el caso G4 fue **verde en ambas** — sin evidencia de cierre débil.

## 3. T9 — LongCat 2.0 medido DE VERDAD (juez v2.3, 2 corridas, estado fresco)

Config verificada en contenedor (`docker exec ... env` + `docker inspect --format {{.Created}}`): `OPENAI_MODEL=meituan/longcat-2.0` vía proxy `scripts/llm-proxy.py :11500` → Nous inference API. Smoke test del proxy OK antes de cada corrida.

| Run | Modelo | Score | Run ID |
|---|---|---|---|
| LongCat A | meituan/longcat-2.0 | **91** | `run_utq7v4kp7kxfj6i2jhem` |
| LongCat B | meituan/longcat-2.0 | **100** | `run_v52ij13gu6e9wbrce8g3` |
| 14B A5/B5 (referencia) | qwen2.5-coder:14b | 82 / 91 | (ver §1) |

Delta LongCat A→B = 9 (mismo nivel de ruido del juez que el 14B). **LongCat iguala o supera al 14B local en score y NO incurrió en rojos por alucinación afirmativa** (1 rojo en A: `Comprador decidido`, juez marca alucinación; en B sin rojos). Latencia: corridas LongCat tardaron más por red (~30-35 min vs ~18 min del 14B local).

**Estado revertido:** override de vuelta al 14B, `nea` recreado y verificado (`OPENAI_MODEL=qwen2.5-coder:14b`, contenedor healthy). **Pendiente de limpieza:** el proceso del proxy sigue escuchando en 127.0.0.1:11500 (PID 24124) — el kill fue bloqueado por el gate de aprobación; inofensivo mientras el `nea` apunte a Ollama, pero matarlo cuando sea posible.

## 4. Incidentes de la sesión

- Docker Desktop se cayó a mitad de la sesión (el OOM conocido): reiniciado, stack levantado con `docker start`, salud verificada, corrida LongCat B repetida desde estado fresco.
- Las corridas LongCat B y A5/B5 incluyen el juez en su versión vigente; ninguna corrida usó modelo distinto al declarado (env verificado antes de cada lanzamiento).

## 5. T10 — parámetro `model` del API del lab

Sin implementar (requiere decisión humana): eliminarlo del contrato/UI o pasarlo de verdad hasta el env del contenedor `nea`. Hoy sigue ignorándose (`route.ts:49`).

## 6. Próximos pasos sugeridos

1. Probar juez 14B (`qwen2.5-coder:14b` como `OPENROUTER_JUDGE_MODEL`) o un juez hosted para cerrar el ruido de ±9.
2. Decidir T10.
3. Commit/push de TODO (regla #3): prompts.ts, guards.py, turn.py, override, eval/*.
4. Matar el proceso del proxy (:11500, PID 24124).

## Cambios en disco (SIN commit)

- `voceroCRM` (agent-qa): `src/server/ai/prompts.ts` (juez v2.3), `eval/judge-prompt-v2.md`, `eval/guarded-20260918/control-{a3,b3,a4,b4,a5,b5}.json`, `eval/guarded-20260918/longcat-{a,b}.json`, `eval/REPORT-20260919.md` (este), `docker-compose.override.yml` (revertido al 14B).
- `agentNEA` (agent-qa): `app/guards.py` (TPL_CIERRE_G4), `app/turn.py` (veto G4 con plantilla).

---

# FASE 3 — Juez free de Nous (LongCat) + ronda Laguna S 2.1:free

Todo verificado por ejecución real; artefactos en `eval/guarded-20260919-juez-free/`.

## F3.1 — Juez sustituido: `meituan/longcat-2.0` vía proxy Nous

- `OPENROUTER_BASE_URL=http://host.docker.internal:11500`, `OPENROUTER_JUDGE_MODEL=meituan/longcat-2.0` en el contenedor `app` (env verificado con `docker exec` + `docker inspect --format {{.Created}}`).
- `scripts/llm-proxy.py` extendido: ahora enruta **cualquier modelo con `/`** al Nous inference API (antes solo `meituan/*`); el resto sigue a Ollama.
- Smoke test del juez OK: JSON limpio (`veredicto`+`hallazgos`), ~8.6 s por llamada, extractable por `extractJson` (bloque ```json).

**Calibración: PASS.** Controles idénticos (agente 14B, estado fresco, juez LongCat):

| Control | Run ID | Score |
|---|---|---|
| A | `run_k229jjdbex17blwcfprs` | 91 |
| B | `run_tyzadnb3qnfrlqewd5pd` | 86 |

**Delta 5 < ±8** — el ruido ±9 del juez 7B local desapareció a nivel de score. Corridas con juez remoto: ~15-19 min (sin penalización notable).

## F3.2 — LongCat 2.0 como agente, bajo el juez nuevo

| Run | Score | Run ID |
|---|---|---|
| LongCat agente | **91** | `run_qnnkmnqpvwomi4ocegsw` |

## F3.3 — Ronda Laguna S 2.1:free (solicitud del humano)

ID exacto verificado en el catálogo Nous (`/v1/models`): `poolside/laguna-s-2.1:free` (existe también la variante pagada `poolside/laguna-s-2.1`). Smoke test vía proxy OK (~6 s, modelo con reasoning pero devuelve `content`).

| Corrida | Score | Veredictos | Run ID |
|---|---|---|---|
| Laguna A | **82** | 8 verde, 2 amarillo, **1 rojo** (alucinación ×3 en `Preguntón de precios`) | `run_1v2l2p849ngqrmgi8kgi` |
| Laguna B | **100** | 11 verde | `run_7hwf19t29sl2e4ro89zn` |

Delta A→B = 18: mayor que LongCat agente (9) y que 14B (5-9) **bajo el mismo juez** → la varianza aquí es del AGENTE Laguna, no del juez. El rojo de A es una alucinación afirmativa real en precios (`Preguntón de precios`), no ruido del juez. Lectura: Laguna tiene techo 100 pero es menos estable; LongCat sigue siendo el mejor agente hosted.

## Estado final (revertido y verificado)

- `nea`: `OPENAI_MODEL=qwen2.5-coder:14b`, `OPENAI_BASE_URL=...11434/v1` (env verificado; healthy).
- `app`: juez LongCat **dejado activo** (calibrado) — decisión humana pendiente si se vuelve al 7B local. ⚠️ Si se revierte el juez: cambiar `OPENROUTER_BASE_URL` de vuelta a `http://host.docker.internal:11434` y quitar `OPENROUTER_JUDGE_MODEL`.
- Proxy `scripts/llm-proxy.py` escuchando en 127.0.0.1:11500 (PID 26888) — **necesario mientras el juez sea LongCat**; matarlo solo si se revierte el juez.
- Stack healthy; `curl -sk https://localhost/api/health` OK.

## Pendientes

1. Decisión humana: ¿juez LongCat definitivo? (delta 5, recomendado)
2. T10 sin cambios (requiere decisión humana).
3. Commit/push de TODO (regla #3) — sigue pendiente de autorización escrita.
4. Actualizar skill `vocero-nea-qa` (hecho: tabla de modelos con juez free y Laguna).

---

# FASE 4 — Anti-alucinación de datos de producto (G6) + regla de prompt

Implementado por Claude Sonnet (delegación), revisado y validado por ejecución real. Artefactos en `eval/guarded-20260920-g6/`.

## F4.1 — Cambios (agentNEA, rama agent-qa, SIN commit)

- `app/prompt.py`: 2 reglas nuevas en NUNCA — prohibido afirmar principio activo/precio/stock que no venga de herramienta en ESTE turno; prohibido narrar intenciones de búsqueda sin ejecutar la herramienta ("llama primero, habla después").
- `app/guards.py`: **G6** — `AFIRMA_DATO_CATALOGO` (regex: precios $/Bs, stock "hay N unidades", "el/su principio activo es X") + `TPL_VERIFICACION_CATALOGO` (plantilla constante) + `afirma_dato_catalogo()`.
- `app/turn.py`: veto G6 integrado a la cadena de guardas (G1→G5→G6): si no hubo resultados de catálogo en el turno y el texto afirma datos de producto → reemplazo por plantilla. G4 intacto.

Verificación: 7 asserts de regex OK en host, `py_compile` OK, imports de `guards`+`turn`+`prompt` limpios DENTRO del contenedor reconstruido (`--force-recreate --build nea`, env verificado).

## F4.2 — Validación (agente 14B, juez LongCat, estado fresco, TRUNCATE antes de cada corrida)

| Corrida | Score | Veredictos | Run ID |
|---|---|---|---|
| G6-A | **91** | 9 verde, 2 amarillo (ambos por TONO, cero alucinaciones) | `run_9uw7wec69l8ghlwaud3a` |
| G6-B | **91** | 10 verde, 1 rojo (ver F4.3) | `run_tm5igm7qxt4frjcifgte` |

**Delta 0** (antes: 82↔91 con mismos modelos). `Preguntón de precios` pasó de rojo intermitente → amarillo/verde: en A el agente respondió honestamente que no tiene info y no inventó. **Cero hallazgos de alucinación de precio/composición en 2 corridas.**

## F4.3 — Modo de falla nuevo descubierto: ECHO

En G6-B, `Comprador decidido` fue rojo por alucinación con evidencia legítima: el agente **repitió literalmente el mensaje del cliente** ("Perfecto, quiero 2 cajas de losartán 50 mg.") en vez de confirmar el pedido/ejecutar la acción. No es dato de catálogo inventado (G6 no aplica); es confusión de rol/eco. **Siguiente palanca: regla de prompt anti-eco o guarda G7 que detecte `final_text` casi idéntico al último mensaje del cliente.**

## Notas operativas

- 2 runs huérfanos de un kernel colgado (`run_vap3lmt7muxgin8v27ps`=86 y `run_2sn2pjmmdephctdctmih`=judge_failed×11, este último ANTES de la reconstrucción de nea): ignorar, no son comparables.
- Juez LongCat se mantuvo activo durante toda la fase; proxy :11500 vivo.

---

# FASE 5 — Guarda G7 anti-eco

Implementada por Claude Sonnet (delegación), revisada y validada por ejecución real. Artefactos en `eval/guarded-20260920-g7/`.

## F5.1 — Cambios (agentNEA, rama agent-qa, SIN commit)

- `app/guards.py`: **G7** — `es_echo(texto_agente, texto_cliente)` (normalización minúsculas/acentos/puntuación + solapamiento de tokens bidireccional >70%, mínimo 4 tokens) + `TPL_NO_ECHO`.
- `app/turn.py`: veto G7 como último eslabón de la cadena (G2→G3→G1→G1-ext→G5→G6→**G7**).
- `app/prompt.py`: regla NUNCA nueva — prohibido repetir/parafrasear el mensaje del cliente como respuesta; actuar o pedir el dato que falta.

Verificación: 6 asserts en host (eco del caso QA detectado; saludos y respuestas legítimas no disparan), imports limpios dentro del contenedor reconstruido, `Comprador decidido` dejó de tener eco en ambas corridas.

## F5.2 — Validación (agente 14B, juez LongCat, estado fresco)

| Corrida | Score | Veredictos | Run ID |
|---|---|---|---|
| G7-A | **86** | 9 verde, 1 amarillo (tono), 1 rojo (ver F5.3) | `run_t9vjltl4aci5yzerjppi` |
| G7-B | **95** | 10 verde, 1 amarillo (tono) | `run_lyc5g0bkiyityzds0l3a` |

Delta 9 — por encima del umbral de calibración (8), pero el rojo de A NO es ruido del juez: es un modo de falla nuevo y genuino (F5.3). **El eco quedó eliminado** (cero veredictos por eco en ambas corridas); `Comprador decidido` estable en amarillo por tono.

## F5.3 — Modo de falla nuevo: fuga de placeholder de herramienta

En G7-A, `Errores y modismos` fue rojo porque el agente **escribió literalmente la plantilla de la herramienta**: "...aquí tienes la información: [inserta información del producto desde buscar_medicamento]" — imitó la estructura del tool-call sin ejecutarlo. Determinista y barato de vetar: **G8** (regex `\[inserta[^\]]*\]` / placeholders entre corchetes en `final_text` → reemplazo por plantilla de búsqueda honesta).

## Próximos pasos

1. **G8 anti-placeholder (IMPLEMENTADA, pendiente de validación)**: `tiene_placeholder()` + `TPL_BUSQUEDA_HONESTA` en `app/guards.py`, veto en `app/turn.py` tras G7, regla NUNCA en `app/prompt.py`. Verificada: asserts de regex OK, `py_compile` OK, imports limpios en contenedor reconstruido (`nea` healthy, Created 2026-09-20T14:35Z). Falta: 2 corridas del lab con estado fresco.
2. Commit/push de TODO (fases 3-5 + G8 + artefactos) — pendiente de autorización escrita.
3. T10 sin cambios.

---

# FASE 6 — Validación de G8 (anti-placeholder) y cierre de fases 3-5

Implementada en fase 5, validada aquí por ejecución real. Artefactos en `eval/guarded-20260920-g8/` (`g8-a.json`, `g8-b.json`). Agente `qwen2.5-coder:14b` local, juez LongCat vía proxy :11500, TRUNCATE antes de cada corrida (autorizado), estado fresco.

## F6.1 — Corridas

| Corrida | Score | Veredictos | Run ID |
|---|---|---|---|
| G8-A | **86** | 8 verde, 3 amarillo (todos por TONO), 0 rojo | `run_5wrwg4tyhnceaz7kf8p2` |
| G8-B | **86** | 9 verde, 2 amarillo (tono + debió escalar), 0 rojo | `run_npob36fkzttcukt6vs4k` |

**Delta 0. PASS.** Criterios cumplidos en ambas corridas:

- `Errores y modismos` sin rojo por placeholder (A: verde; B: amarillo solo por tono — no hay fuga de plantilla).
- **Cero alucinaciones de precio/composición** (G6 estable).
- **Cero eco** del mensaje del cliente (G7 estable).
- **Cero fugas de placeholder** de herramienta (G8 estable).
- Amarillos: tono seco/robótico (3), un `debio_escalar` en `Pide un humano` (B) donde el agente se despidió en vez de hacer handoff — rojo legítimo de escalado, no alucinación; ya existía como caso límite conocido.

## F6.2 — Conclusión de las fases 3-5 (cadena de guardas G1-G8)

| Guarda | Anti | Verificación | Corridas de validación | Resultado |
|---|---|---|---|---|
| G1 | precio sin dato del catálogo | lab | históricas | estable, verde |
| G2 | fuera de KB | lab | históricas | estable, verde |
| G3 | media fantasma | lab | históricas | estable, verde |
| G4 | acusación de fraude (cierre con plantilla) | F2 | A5/B5 + G6/G7/G8 | estable, verde |
| G6 | datos de catálogo afirmados sin herramienta | F4 | G6: 91/91 (delta 0) | PASS, delta 0 |
| G7 | eco del mensaje del cliente | F5 | G7: 86/95 | eco eliminado; rojo de A = placeholder (motivó G8) |
| G8 | placeholder de herramienta en `final_text` | F6 | G8: 86/86 (delta 0) | **PASS, delta 0** |

Tendencia del score con agente 14B + juez LongCat, cadena completa: 91/91 (G6) → 86/95 (G7) → 86/86 (G8). La varianza inter-run desapareció en G8 (delta 0) y **no queda ningún modo de falla de alucinación activo**: los amarillos restantes son tono/severidad del juez, no fugas del agente. El caso `Pide un humano` con cierre seco (B) es el único hallazgo residual — **documentado como candidato a mejora (plantilla de cierre de escalado amable, tipo G4), NO implementado sin orden.**

## Próximos pasos

1. Commit/push de TODO (fases 3-5 + G8 + artefactos) — pendiente de autorización escrita.
2. T10 sin cambios (decisión humana).
3. Decisión humana sobre el cierre seco de `Pide un humano` (candidato a guarda/plantilla futura).
