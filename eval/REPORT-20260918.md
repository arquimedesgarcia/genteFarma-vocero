# REPORT-20260918 — Ciclo QA anti-alucinación NEA (rama agent-qa, SIN commit)

## 1. Veredicto T1 — CONFIRMADA con agravante

El campo `model` del POST `/api/lab/runs` **se ignora por completo**:
- `src/app/api/lab/runs/route.ts:49` — `startRun(session.organizationId)`: el body nunca se lee.
- `src/server/lab/runner.ts:25` — `startRun(organizationId: string)`: sin parámetro de modelo.
- El modelo del agente vive SOLO en el env del contenedor `nea` (`OPENAI_MODEL`), y el juez en `OPENROUTER_JUDGE_MODEL` del `app`.

**Bug documentado: parámetro engañoso del API del Laboratorio.** Además, el run del 17-sep etiquetado "7B" corrió con el 14B y el run etiquetado "LongCat" también: las diferencias 29→14→21 eran **ruido + fuga de estado, no cambios de modelo**.

## 2. Estado del proxy y del override

- Proxy `scripts/llm-proxy.py` (:11500, LongCat→Nous / resto→Ollama): **APAGADO**. Reactivar: `python scripts/llm-proxy.py 11500` (host) y poner `OPENAI_MODEL=meituan/longcat-2.0` + `OPENAI_BASE_URL=http://host.docker.internal:11500/v1` en el servicio `nea` del override. Requiere token válido en `%LOCALAPPDATA%\hermes\shared\nous_auth.json`.
- Override revertido al **baseline 14B** (qwen2.5-coder:14b + Ollama directo).
- **Corrección de honestidad:** el run "LongCat 21/100" del ciclo anterior usó el 14B (contenedor nunca recreado). LongCat sigue **candidato sin medir**. Su API funciona: `meituan/longcat-2.0` en `https://inference-api.nousresearch.com/v1` (modelo free #1 del Portal por uso diario).

## 3. Ruido del juez (T3): delta 0 — criterio ±8 CUMPLIDO

| Corrida | Run ID | Score |
|---|---|---|
| Control A2 (14B, temp 0) | `run_si2sivzqcd6p02f9oabi` | 14 |
| Control B2 (idéntico) | `run_t8u7fvax3f88btep1fgh` | 14 |

Cambios: `temperature: 0` en el juez (`src/lib/ai/index.ts` + `judge.ts`), prompt versionado en `eval/judge-prompt-v1.md`.

**Descubrimiento crítico — fuga de estado entre corridas:** NEA persiste el estado conversacional de los teléfonos de prueba en su Postgres; corridas sucesivas heredan conversaciones cerradas → el agente responde con silencio → todo rojo (corridas de 0/100 eran esto). Procedimiento obligatorio antes de CADA corrida:

```bash
docker exec vocerocrm-nea-db-1 psql -U postgres -d nea -c \
  'TRUNCATE bot_conversation, bot_message, bot_cart, processed_message, relay_queue, pending_send RESTART IDENTITY CASCADE;'
```

**Descubrimiento crítico 2 — el Laboratorio corría sin modo farmacia:** la org de QA3 tenía `provider_id` vacío → `farmacia=False` → sin catálogo ni herramientas (el agente pedía precios al CLIENTE). Corregido: `UPDATE organization SET provider_id='demo' WHERE id='org_eh8sds294yxw3mebiy6o'`. El baseline 29/100 se midió sobre el agente degradado.

También: timeout global del runner 10→30 min (`runner.ts:21`, el modo farmacia real necesita ~15-18 min para 11 casos).

## 4. Guardas (T4) — estado y evidencia

Módulo nuevo `agentNEA/app/guards.py` + cableado en `app/turn.py` (import, señal G3 al prompt, vetos post-generación, G4 pre-handoff). Detectores con 12 asserts unitarios OK.

| Guarda | Estado | Evidencia (run final `run_kksa3qsz9uopy86et7a8`) |
|---|---|---|
| G1 precio sin catálogo | **FUNCIONA** | 2º turno sin cifra inventada; plantilla de rechazo ("no encontré paracetamol en catálogo") — requiere `pide_precio` + precio en texto + `last_products` vacío |
| G2 fuera de KB | **FUNCIONA** | VETO ×2 en log (13:22:38 "devolverlas", 13:05:22 "vencido"); caso G2 **VERDE** con plantilla textual |
| G3 media fantasma | **FUNCIONA** | Señal inyectada (3×); agente jamás afirma la foto ("no puedo recibir fotos... escriba los nombres"); `Receta por foto` **VERDE** (antes rojo por "Recibí tu foto") |
| G4 hostilidad/fraude | **FUNCIONA** (parcial) | VETO ×2, handoff inmediato en el log (13:23:58); la redacción del cierre sigue saliendo débil del LLM (verificado en transcript) |

Iteraciones de regex durante el ciclo: `ladron(es)` plural; `devolverlas` prefijo; formato de precio "5 Bs" (número ANTES de Bs).

## 5. Scores comparativos

**IMPORTANTE — las composiciones difieren; comparar con cautela:**

| Run | Config | Casos | Score |
|---|---|---|---|
| baseline 20260917 | 14B, modo degradado (sin catálogo), estado fresco | 7 | 29 |
| Controles A2/B2 | 14B, modo degradado, juez temp 0 | 7 | 14 / 14 |
| pre-guardas (farmacia on) | 14B + catálogo | 11 | 18 (timeout en 1ª; `run_oy91bjkde9q7kgs964s6` = 9) |
| **guarded-final** | 14B + catálogo + guardas G1-G4 | 11 | **27** (`run_kksa3qsz9uopy86et7a8`) |

Dentro de las 7 personas originales, guarded-final: `Receta por foto` rojo→**VERDE**, resto rojos (4 de ellos con el agente comportándose CORRECTAMENTE y el juez marcando igual — ver §6).

Artefactos: `eval/guarded-20260918/guarded.json` (9/100, pre-fix regex), `guarded-final.json` (27/100), `control-a2.json`, `control-b2.json`.

## 6. Vetos (fallas críticas, separadas del score)

Vetos de guarda activados en guarded-final (log NEA): **G2 ×2, G4 ×1, G1 ×0 (no hubo precio inventado), G3 señal ×3 sin veto post** (el veto G3 post disparó en la corrida 9/100: "el agente afirmó recibir media inexistente").

**Problema de juez detectado (pendiente del addendum F4):** en guarded-final, los casos G1, G3 y G4 tienen al agente comportándose correctamente (sin cifras inventadas, sin afirmar fotos, handoff ejecutado) y aun así el juez los marca `rojo/alucinación`. Ejemplo: G3 "Como soy una IA basada en texto, no puedo ver fotos" → marcado `fuera_de_kb`. El juez confunde **rechazo honesto** con alucinación. El addendum del prompt del juez (`f4-calibration.md`) debe priorizar: "un rechazo/escalado honesto NO es alucinación".

## 7. Próximos pasos

1. **Ajustar el juez** (addendum F4): rechazo honesto/escalado ≠ alucinación; exigir cita textual del transcript para cada hallazgo. Re-calibrar con 2 controles (delta < ±8).
2. **Endurecer G4:** hoy el handoff ocurre (log) pero la redacción del cierre la genera el LLM; usar plantilla constante de cierre + handoff.
3. **Medir LongCat de verdad** (2 corridas con estado fresco, juez temp 0) — candidato free #1 del Portal.
4. **Decidir sobre el parámetro `model`** del API del lab: eliminarlo del UI/contrato o implementarlo de verdad.
5. Commit/push de TODO lo anterior — pendiente de autorización (regla #3).

## Cambios en disco (sin commit)

- `voceroCRM` (agent-qa): `src/lib/ai/index.ts`, `src/server/lab/judge.ts`, `src/server/lab/runner.ts`, `src/server/lab/personas.ts` (+4 casos), `eval/judge-prompt-v1.md`, `eval/judge-calibration-20260918.md`, `eval/guarded-20260918/`, `eval/baseline-20260917/{control-a2,control-b2,run7b,runlongcat}.json`, `eval/scenarios/{kn,rb,es}_*.yaml` (+4), `scripts/llm-proxy.py`, `docker-compose.override.yml` (revertido).
- `agentNEA` (agent-qa): `app/guards.py` (nuevo), `app/turn.py` (import + G3 señal + G4 inmediato + vetos post).
- BD QA: `organization.provider_id='demo'` para la org de QA3 (config de entorno, no código).
